#!groovy
node {
   stage('Git Checkout') {
        checkout scm
   }
   stage('Run Matlab') {
        try {
            bat('example.bat')
        } finally {
            archive('*.log')
        }
   }
}