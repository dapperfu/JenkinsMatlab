#!groovy
node {
   stage('Checkout') {
        checkout scm
   }
   stage('Build') {
        try {
            bat('example_build.bat')
        } finally {
            archive '*.log' '*_ert_rtw/*'
        }
   }
}